---
- hosts: hosts
  become: true

  var_files:
    - vars.yml

  tasks:
  - name: install updates
    package:
      upgrade: dist
      update_cache: yes
      # doesn't count as change:
      changed_when: false

- hosts: Avell_in_Popoloninnis
  become: true
  connection: local
  tasks:

  - name: install packages
    tags: livecoding
    ansible.builtin.apt:
      name: "{{ package_names }}"
      state: latest
    # when: ansible_distribution in ["Debian", "Ubuntu"]

  - name: copy wallpaper file
    copy:
      src: files/wallpaper.png
      dest: /usr/share/backgrounds/vim_cheatsheet.png
      owner: root
      group: root

  - name: set wallpaper
    dconf:
      key: "/org/gnome/desktop/background/picture-uri"
      value: "'file:///usr/share/backgrounds/vim_cheatsheet.png'"

  - name: set wallpaper position
    dconf:
      become_user: skmecs 
      key: "/org/gnome/desktop/background/picture-options"
      value: "'zoom'"

  - name: add ansible user
    tags: always
    user:
      name: exu
      system: yes

  - name: set up sudo for ansible user
    copy:
      src: files/sudoer_exu
      dest: /etc/sudoers.d/exu
      owner: root
      group: root
      mode: 0440

  - name: add ansible-pull cron job
    cron:
      name: ansible auto-provision
      user: exu
      minute: "*/10"
      job: ansible-pull -o -U "https://github.com/gilfuser/ansible_laptop.git"



